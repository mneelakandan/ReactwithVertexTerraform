{
  "name": "vertex-react-app-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:server": "node server.js",
    "dev:client": "cd client && npm start",
    "build": "cd client && npm ci && npm run build && cp -r build ../",
    "start": "node server.js"
  },
  "dependencies": {
    "@google-cloud/vertexai": "^1.9.0",
    "cors": "^2.8.5",
    "express": "^4.18.3"
  },
  "engines": {
    "node": ">=20"
  }
}
